console.log 'devise success'
$('.modal').modal('hide');

if($('#flash').length > 0) {
 $('#flash').remove();
 $('.navbar').prepend("<%= escape_javascript raw(flash_normal) %>");
}
else {
 $('.modal').modal('hide');
 $('navbar').prepend("<%= escape_javascript raw(flash_normal) %>");
}

$('#flash').hide().fadeIn(1000).delay(2000).fadeOut(1000)