$('.modal').modal('hide')

if($('#flash').length > 0) {
 $('#flash').remove();
 $('.navbar').prepend("<%= escape_javascript raw(flash_normal) %>");
}
else {
 $('.modal').modal('hide')
 $('body').prepend("<%= escape_javascript raw(flash_normal) %>");
}

$('#flash').hide().fadeIn(1000).delay(3000).fadeOut(1000)