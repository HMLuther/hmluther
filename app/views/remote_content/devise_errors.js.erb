if($('.modal-body #flash').length > 0) {
 $('.modal-body #flash').remove();
 $('.modal-body').prepend("<%= escape_javascript raw(flash_normal) %>");
}
else {
 $('.modal-body').prepend("<%= escape_javascript raw(flash_normal) %>");
}