console.log('Added to collection')

if($('#flash').length > 0) {
 $('#flash').remove();
 $('.flash-ajax').prepend("<%= escape_javascript raw(flash_normal) %>");
}
else {
 $('.flash-ajax').prepend("<%= escape_javascript raw(flash_normal) %>");
}

$('#flash').hide().fadeIn().delay(1000).fadeOut()

// $('.flash-ajax').html('Item #<%= @collection_item.item.filemaker_id %> added to <%= @collection_item.collection.name %>.')
// $('.flash-ajax').fadeIn(500).delay(3000).fadeOut(500)

